apiVersion: v1
kind: Service
metadata:
  name: {{ include "app-ui.fullname" . }}
  labels:
    app: {{ include "app-ui.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ include "app-ui.name" . }}
